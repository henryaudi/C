/**
 * This is a simple C program that demonstrates basic arithmetic operations and output formatting.
 * It calculates the sum of two integers and prints the result to the console.
 *
 * @author Henry
 * @date 02/27/2026
 * @version 1.0
 */
#include <stdio.h>

int main()
{
  double x;
  int a, b, c;

  // We can assign values to the integers a, b, and c with = operator
  a = 5;
  b = 10;
  c = 12;

  scanf("%lf", &x);
  printf("The value of x is: %.2lf\n", x);

  return 0;
}
